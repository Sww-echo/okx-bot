# ==========================================
# OKX 网格交易机器人 - 环境变量配置模板
# ==========================================
# 使用方法：
# 1. 复制此文件为 .env
# 2. 填写您的真实配置
# 3. 请勿将 .env 文件提交到版本控制
# ==========================================

# ==================== 实盘 API (FLAG=0 时使用) ====================
# 在 OKX 官网 -> 个人中心 -> API 管理 中创建
OKX_API_KEY=your_api_key_here
OKX_SECRET_KEY=your_secret_key_here
OKX_PASSPHRASE=your_passphrase_here

# ==================== 模拟盘 API (FLAG=1 时使用) ====================
# 在 OKX 模拟交易环境中创建的 API
# 如果不需要模拟盘，可留空，系统会回退使用实盘 API
OKX_DEMO_API_KEY=1f5b13e7-9a6f-4963-b224-c07d2b1172e1
OKX_DEMO_SECRET_KEY=FDC17A9594D9A38B0BC785CBF9BFDAE5
OKX_DEMO_PASSPHRASE=Aa123467.

# ==================== 通知配置 - 钉钉机器人 (可选) ====================
# 钉钉群 -> 设置 -> 智能群助手 -> 添加机器人 -> 自定义(通过Webhook接入)
# Webhook 地址（必填）
DINGTALK_WEBHOOK=
# 加签密钥（可选，如果机器人安全设置选择了"加签"，则填写 Secret）
DINGTALK_SECRET=

# ==================== 通知配置 - 企业微信机器人 (可选) ====================
# 企业微信群 -> 添加群机器人 -> 复制 Webhook 地址
WECHAT_WEBHOOK=

# ==================== 通知配置 - Bark (iOS推送) ====================
# 下载 Bark App -> 复制 URL 中的 Key (例如 https://api.day.app/YourKey/...)
BARK_KEY=
# Bark 服务器地址 (可选，默认为官方服务器 https://api.day.app)
BARK_SERVER=https://api.day.app


# ==================== Web控制面板配置 ====================
# 设置密码以启用安全认证 (留空则不启用)
WEB_USER=admin
WEB_PASSWORD=


# ==================== 交易模式配置 ====================
# 交易类型: spot (现货) 或 swap (永续合约)
BASE_SYMBOL=ETH
TRADE_MODE=swap

# ==================== 合约配置（仅 TRADE_MODE=swap 时生效）====================
# 保证金模式: cross (全仓) 或 isolated (逐仓)
MARGIN_MODE=cross
# 持仓方向: net (单向持仓) 或 long/short (双向持仓时指定方向)
POS_SIDE=long
# 杠杆倍数 (1-125)
LEVERAGE=20

# ==================== 交易配置 ====================
# 初始基准价格 (USDT)
# 如果设置为 0，系统将使用启动时的实时价格
INITIAL_BASE_PRICE=0.0

# 初始本金 (USDT)
# 用于计算总盈亏和收益率
INITIAL_PRINCIPAL=1000.0
